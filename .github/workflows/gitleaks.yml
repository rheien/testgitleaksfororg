name: gitleaks

on: [push, pull_request, workflow_dispatch]

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: gitleaks-action with 
        with: 
          repository: spring-media/red-flux-cd
        uses: rheien/gitleaks-action@master
